{
  "name": "otb-scout-ai",
  "version": "1.3.1",
  "private": true,
  "description": "Cache-first OTB team role intelligence Worker",
  "main": "worker.js",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "audit:discovery": "node scripts/audit-live-discovery.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
